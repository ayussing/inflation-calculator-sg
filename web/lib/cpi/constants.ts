// The headline "All Items" series code, produced by the CSV parser's slugifier for the root row.
export const ALL_ITEMS_CATEGORY_CODE = "all-items";

// Division-level categories are direct children of "all-items" (depth 1 in the parsed hierarchy).
export const DIVISION_LEVEL = 1;
